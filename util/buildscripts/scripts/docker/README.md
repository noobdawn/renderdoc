This folder creates a docker container pre-configured for compiling NoobDawn with the oldest possible distribution, for maximum compatibility of the binary build.

It doesn't contain anything NoobDawn-specific, apart from being tailored to its dependencies.
